# Peer dependencies of the @ocelescope/* packages (Mantine, React Query, etc.)
# are installed automatically. This is pnpm's default since v8; set explicitly
# here so the behaviour is obvious and stable.
auto-install-peers=true
